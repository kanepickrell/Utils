#library reqs
None

#plan:
#create a method for taking an array and implement a bubble sort

def bubbleSort(array):
    n = len(array)
    for i in range(n):
        for j in range(0, n - i - 1):
            if array[j] > array[j + 1]:
                array[j], array[j + 1] = array[j + 1], array[j]

    return print(array)

bubbleSort([22,44,11,2,4,77,3])